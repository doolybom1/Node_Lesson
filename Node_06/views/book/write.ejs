<!DOCTYPE html>
<html>
<head>
    <%- include ('../include-header') %>
    <script>
        $(function(){
            $(".btn-delete").click(function(){
                if(confirm("삭제할까요?")){
                    document.location.replace("/book/delete/<%- book._id %>")
                }
            })
            $(".btn-list").click(function(){
                document.lcoation.replace("/book")
            })
        })
    </script>
</head>
<body>
    <form method="POST" class="container">
        <fieldset>
        <legend>
            <% if(formTitle == 'UPDATE') { %>
                도서정보 수정
            <% }else{ %>
                도서정보 추가
            <% } %>
        </legend>
            <label class="w3-text-blue" for="bTitle"><b>도서명</b></label>
            <input type="text" id="bTitle" name="bTitle" value="<%- book.bTitle %>" class="w3-input w3-border">
            
            <label class="w3-text-blue" for="bWriter"><b>저자</b></label>
            <input type="text" id="bWriter" name="bWriter" value="<%- book.bWriter %>" class="w3-input w3-border">
            
            <label class="w3-text-blue" for="bComp"><b>출판사</b></label>
            <input type="text" id="bComp" name="bComp" value="<%- book.bComp %>" class="w3-input w3-border">
            
            <label class="w3-text-blue" for="bPrice"><b>가격</b></label>
            <input type="text" id="bPrice" name="bPrice" value="<%- book.bPrice %>" class="w3-input w3-border">

            <button class="w3-button w3-blue w3-right w3-round w3-margin-top">저장</button>

            <% if(formTitle == 'UPDATE') { %>
                <button type="button" class="w3-button w3-red w3-right w3-round w3-margin-top w3-margin-right btn-delete">삭제</button>
                <button type="button" class="w3-button w3-green w3-right w3-round w3-margin-top w3-margin-right btn-list">리스트</button>
            <% }else{ %>
            <% } %>



        </fieldset>
    </form>
</body>
</html>